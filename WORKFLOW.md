# Default Workflow

Use this repository as a backup point for the current workspace.

1. Make your changes wherever you normally work.
2. Run `.\scripts\Backup-Workspace.ps1` whenever you want a checkpoint.
3. Use Git history for undoing work, not ad hoc copies.

This keeps the workspace backed up locally and on GitHub without forcing new projects into one repository root.
